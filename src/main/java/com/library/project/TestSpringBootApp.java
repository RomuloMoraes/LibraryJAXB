/*
package com.library.project;

import com.library.project.repository.StudentRepository;
import com.library.project.repository.RequestRepository;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.ConfigurableApplicationContext;
import com.library.project.repository.BookRepository;

@SpringBootApplication
public class TestSpringBootApp {
    public static void main(String[] args) {
        ConfigurableApplicationContext configurableApplicationContext = SpringApplication.run(TestSpringBootApp.class);
        BookRepository book = configurableApplicationContext.getBean(BookRepository.class);
        StudentRepository student = configurableApplicationContext.getBean(StudentRepository.class);
        RequestRepository request = configurableApplicationContext.getBean(RequestRepository.class);


    }
}
*/
